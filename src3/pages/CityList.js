import React from 'react';
import {View, Text, SafeAreaView} from 'react-native';

const CityList = (props) => {
  return (
    <SafeAreaView>
      <View>
        <Text>CityList</Text>
      </View>
    </SafeAreaView>
  );
};

export {CityList};
